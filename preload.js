// Preload script for security
// This runs in a sandboxed context before the page loads

window.addEventListener('DOMContentLoaded', () => {
  // You can add any DOM manipulation or bridge functions here if needed
});
